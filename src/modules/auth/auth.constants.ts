/** Name of the HttpOnly cookie that carries the session JWT. */
export const SESSION_COOKIE_NAME = "tedi_session";

/** Key used to attach the resolved AuthUser to the Express request object. */
export const REQUEST_USER_KEY = "authUser";
