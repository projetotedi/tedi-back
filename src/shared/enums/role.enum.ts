// Hierarchy order: member < director < coordinator
// roleSatisfies() comparison will be implemented in GUS-75 (auth module).
export enum Role {
  MEMBER = "member",
  DIRECTOR = "director",
  COORDINATOR = "coordinator",
}
